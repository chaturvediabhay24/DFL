Image: dtoc_flowchart.svg

Caption: DTOC flow — store tool output, hide with a placeholder, and restore on demand.

Alt text: Flowchart with four steps: (1) Agent calls a tool, (2) system stores the full output with an ID, (3) context shows a short placeholder and hides the full output, (4) agent re-enables the output on demand to restore the original content.

Short paste text to put under image on Medium:

DTOC flow: the agent calls a tool and the system saves the full output with an ID. The active context shows only a small placeholder so the agent stays focused. When needed, the agent restores the full content by enabling the ID — nothing is lost and there is no re-run.
